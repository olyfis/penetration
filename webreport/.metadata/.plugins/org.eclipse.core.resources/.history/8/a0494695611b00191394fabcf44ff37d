// A $( document ).ready() block.
$( document ).ready(function() {
    console.log( "ready!" );
    $.ajax({
    	type: 'Get',
        url: 'http://localhost:8181/webreport/getchart',
        dataType: "json",
        success: function(data) {
        	console.log(data);
        	var arr1 = [];
        	var arr2 = [];
        	
        	for (var i in data) {
        		arr1.push("ID" + data[i].id)
        	}
        	
        	
        },
        error:	function(data) {
        	console.log(data);
        },
    	
    });
    
    
    
});